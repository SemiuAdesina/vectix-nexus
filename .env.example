# =============================================================================
# Vectix Nexus – single .env for Docker Compose (VPS e.g. Hostinger)
# Copy to .env at REPO ROOT (next to docker-compose.yml). Used by: db, backend, frontend, agent.
# Do not use backend/.env or frontend/.env for Docker; this root .env is the only one.
# Production: use live keys. VPS: set MOCK_FLY_DEPLOY=true; do not set ENABLE_NARRATIVE_DEMO or ALLOW_DEPLOY_WITHOUT_SUBSCRIPTION.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Compose builds DATABASE_URL from these for backend)
# -----------------------------------------------------------------------------
POSTGRES_USER=vectix
POSTGRES_PASSWORD=
POSTGRES_DB=vectix_nexus

# -----------------------------------------------------------------------------
# Public URLs (domain: vectixfoundry.com; use HTTPS with Nginx + Let's Encrypt)
# NEXT_PUBLIC_API_URL: frontend only. VPS: leave empty so browser uses relative /api/* (Nginx proxies to backend).
# FRONTEND_URL / CORS_ORIGIN: backend (CORS, redirects).
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=
FRONTEND_URL=https://vectixfoundry.com
CORS_ORIGIN=https://vectixfoundry.com,https://www.vectixfoundry.com
TRUSTED_ORIGINS=https://vectixfoundry.com,https://www.vectixfoundry.com

# -----------------------------------------------------------------------------
# Auth (Clerk)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# -----------------------------------------------------------------------------
# Payments (Stripe)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_HOBBY_PRICE_ID=
STRIPE_PRO_PRICE_ID=

# -----------------------------------------------------------------------------
# Solana (backend + agent)
# -----------------------------------------------------------------------------
SOLANA_RPC_URL=
WALLET_MASTER_SECRET=
TREASURY_WALLET_ADDRESS=

# -----------------------------------------------------------------------------
# Encryption (required for agent secrets / API keys)
# -----------------------------------------------------------------------------
SECRETS_ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# Optional – Opik (Comet ML) observability
# -----------------------------------------------------------------------------
# OPIK_API_KEY=
# OPIK_PROJECT_NAME=vectix-foundry
# OPIK_WORKSPACE_NAME=

# -----------------------------------------------------------------------------
# Optional – TEE / Phala
# -----------------------------------------------------------------------------
# TEE_PROVIDER=phala
# PHALA_API_KEY=
# PHALA_ENDPOINT=https://api.phala.network

# -----------------------------------------------------------------------------
# Optional – paid APIs (leave unset for free tier)
# -----------------------------------------------------------------------------
# LUNARCRUSH_API_KEY=
# DEXSCREENER_API_KEY=
# RUGCHECK_API_KEY=
# GOPLUS_API_KEY=

# -----------------------------------------------------------------------------
# Optional – limits & Redis
# -----------------------------------------------------------------------------
# MAX_TRADE_AMOUNT_SOL=50
# MAX_WITHDRAWAL_PER_TX_SOL=50
# MAX_WITHDRAWAL_PER_DAY_SOL=100
# REDIS_URL=redis://localhost:6379
# RULE_CHANGE_DELAY_MS=3600000

# -----------------------------------------------------------------------------
# Optional – agent character (JSON string; omit to use default)
# -----------------------------------------------------------------------------
# CHARACTER_CONFIG={"name":"MyAgent",...}

# -----------------------------------------------------------------------------
# VPS only: agent runs in Docker. Do not set FLY_*. Set MOCK_FLY_DEPLOY=true.
# -----------------------------------------------------------------------------
MOCK_FLY_DEPLOY=true

# Optional: allow Stripe/Clerk test keys in production until you have live keys.
# Remove or set false before going live.
# ALLOW_TEST_KEYS_IN_PRODUCTION=true

# Optional: demo mode – bypass subscription; Get Started redirects to /create instead of Stripe.
# Use with ALLOW_TEST_KEYS_IN_PRODUCTION=true and ALLOW_DEPLOY_WITHOUT_SUBSCRIPTION=true
# ALLOW_DEPLOY_WITHOUT_SUBSCRIPTION=true
# NEXT_PUBLIC_ALLOW_SUBSCRIPTION_BYPASS=true
